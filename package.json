{
  "name": "@qwikdev/roadplan",
  "version": "0.0.0",
  "description": "Amazing and performant starter for documentation websites powered by Qwik",
  "engines": {
    "node": ">=20.9.0"
  },
  "type": "module",
  "bin": "./lib/index.js",
  "scripts": {
    "build.cli.old": "rimraf lib/ && tsc",
    "build.cli": "rimraf packages/create-roadplan/lib/ && cd packages/create-roadplan/ && tsc",
    "cli": "pnpm build.cli && node ./lib/index.js"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}